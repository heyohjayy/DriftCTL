[scan]
# Store this file in the Terraform project's root. Paths are relative to it.
terraform_dir = "."
aws_profile = "driftctl-readonly"
region = "eu-west-1"
tags = ["Project=Test"]
report = "driftctl-output/live-report.md"
audit = "driftctl-output/events.jsonl"
